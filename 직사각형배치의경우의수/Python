# 몇개의 세로 직사각형이 들어갈 수 있을 지를 입력 
n = int(input())
# dp 테이블 생성 
dp = [0] * (101)
# 점화식으로 계산되지 않은 첫번째와 두번째의 값을 직접 입력 
dp[1], dp[2] = 1, 2

# 반복문을 통해 점화식으로 dp테이블을 채워줌 
for i in range(3, n+1): 
    # 번째 세로 직사각형이 들어갈 수 있는 곳의 경우의수는 i-1 번째와 i-2번째의 경우의수의 합과 같음 
    dp[i] = dp[i-2] + dp[i-1]

// 결과 출력 
print(dp[n] % 1000007)
