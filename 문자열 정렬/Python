# 문자열을 입력받을 2차원 리스트 생성
data = []
# 입력한 숫자의 개수만큼 문자열을 입력받음 
for i in range(int(input())):
    data.append(input())
# sort 함수를 통해 문자열을 오름차순으로 정렬 
data.sort()
# 반복문을 통해서 오름차순대로 정렬된 문자열을 하나씩 출력 
for i in data: print(i)
