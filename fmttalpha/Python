# 1 개씩 늘어가는 광년에서 맨 마지막 이동거리는 1로 고정되어 있는 상황에서 횟수를 구하는 문제 
# 1 개씩 늘어나고 한개씩 줄어드니 그 안에 있는 수는 다 최대 횟수로 고정 
# 예를들면 2는 1 1    3은 1 1 1   4는 1 2 1 이동을 해야하니 2번과 3번의 공간이동 사이에는 최소 값인 3~ 최대 값이 4까지 모든 이동이 포함 

# 출발 지점 n과 도착 지점m을 입력 
n, m = map(int, input().split())

# 총 거리를 계산 및 변수에 저장 
Target = m - n
# 몇번 이동을 햇는지 저장
cnt = 0
# 2가 되면 0으로 초기화 할 숫자의 이동 가능 횟수를 저장 
num_cnt = 0
# 한번당 이동 거리 저장 
num = 1 

# 이동거리를 초과하거나 같을때 까지 반복
while True:
    if Target <= 0: 
        break 
     # 한번당 이동거리를 총 이동거리에서 빼줌
    Target -= num 
    # 이동 가능 횟수를 늘려줌
    num_cnt += 1
    # 이동 가능 횟수가 2가 된 경우 이동가능 횟수를 초기화후 한번당 이동거리를 증가
    if num_cnt == 2:
        num_cnt = 0
        num += 1
    # 이동 횟수를 추가 
    cnt += 1

# 결과 출력 
print(cnt)
