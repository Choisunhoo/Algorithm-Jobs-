#include <stdio.h>
int main() {
    int n; 
    scanf("%d",&n);
    int data[100];

    for (int i=0; i<n; i++) {
        scanf("%d",&data[i]);
    }
    int max = data[0];
    for (int i=1; i<n; i++) {
        if (data[i] > max) max = data[i];
    }
    printf("%d", max);

    return 0;
}
