# 문자열을 넣을 리스트 생성
mystr = list(input())
# 문자열의 개수 초기화
cnt = 0 
# 문자열의 index 초기화
index = 0 

# 반복문 시작
while True:
    # 문자열의 index가 맨 마지막과 같은 경우 반복문 종료
    if mystr[index] == mystr[-1]:
        break
    # 그렇지 않은 경우 문자열의 갯수 추가
    else:
        cnt += 1
    # 반복문이 끝나기 전에 index 추가
    index += 1

# 문자열의 길이 출력
print(index+1)

# or 

# 문자열을 넣을 리스트 생성
mystr = list(input())
# 문자열의 길이 출력
print(len(mystr))
