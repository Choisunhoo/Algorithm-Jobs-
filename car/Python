# 숫자를 하나 지정 후 입력
n = int(input())
# 리스트에 차의 번호를 넣어줌 
data = list(map(int, input().split()))
# 갯수를 세는 변수 초기화
cnt = 0
# 반복문을 통해서 끝자리가 n과 같을 경우 변수에 1추가 
for i in data:
  if i % 10 == n:
    cnt += 1

# 결과 출력 
print(cnt)
