#include <stdio.h>
int main(){
    int a, b, c;
    scanf("%d %d %d",&a, &b, &c);
    if (a > b) {
        int e = a;
        a = b;
        b = e; 
    }
    int cnt = 0;
    for (int i = a; i <=b; i ++) {
        if (i % c == 0) cnt++;
    }
    printf("%d", cnt);
    
    return 0;
}
